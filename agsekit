#!/usr/bin/env python3
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent
PACKAGE_ROOT = ROOT / "agsekit_cli"
if str(PACKAGE_ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))

from agsekit_cli.cli import main

if __name__ == "__main__":
    main()
